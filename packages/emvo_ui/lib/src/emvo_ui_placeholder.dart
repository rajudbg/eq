import 'package:flutter/widgets.dart';

/// Placeholder widget while shared UI components are added.
class EmvoUiPlaceholder extends StatelessWidget {
  const EmvoUiPlaceholder({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
